const fs=require("fs"),path=require("path");const files=[path.resolve("coverage/coverage-summary.json"),path.resolve("frontend/coverage/coverage-summary.json")];for(const file of files){if(!fs.existsSync(file)){console.log(`${file}: not generated`);continue}const total=JSON.parse(fs.readFileSync(file,"utf8")).total;console.log(`${file}: lines ${total.lines.pct}% | functions ${total.functions.pct}% | branches ${total.branches.pct}% | statements ${total.statements.pct}%`)}
